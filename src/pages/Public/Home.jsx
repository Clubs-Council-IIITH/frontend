import Page from "components/Page";

const Home = () => {
    return (
        <Page header={"Home"} loading={false} empty={false}>
            <h1> TODO </h1>
        </Page>
    );
};

export default Home;
